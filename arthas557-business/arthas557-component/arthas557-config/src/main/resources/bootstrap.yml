spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        server-addr: nacos.com:8848
        file-extension: yaml
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true